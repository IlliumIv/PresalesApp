@using PresalesApp.Shared

<RadzenDropDown Name="DepartmentDropDown" TValue="Department" Value="@Department"
                ValueChanged="@(department => _OnDepartmentChanged(department))" Data="@_Departments">
    <Template>
        @(Localization[$"{nameof(PresalesApp.Shared.Department)}{(Department)context}Text"])
    </Template>
    <ValueTemplate>
        @(Localization[$"{nameof(PresalesApp.Shared.Department)}{(Department)context}Text"])
    </ValueTemplate>
</RadzenDropDown>