@using PresalesApp.Shared

<RadzenDropDown Name="KpiCalculationTypeDropDown" TValue="KpiCalculation" Value="@KpiCalculationType"
                ValueChanged="@(calculationType => _OnCalculationTypeChanged(calculationType))" Data="@_CalculationTypes">
    <Template>
        @(Localization[$"{nameof(KpiCalculation)}{(KpiCalculation)context}Text"])
    </Template>
    <ValueTemplate>
        @(Localization[$"{nameof(KpiCalculation)}{(KpiCalculation)context}Text"])
    </ValueTemplate>
</RadzenDropDown>