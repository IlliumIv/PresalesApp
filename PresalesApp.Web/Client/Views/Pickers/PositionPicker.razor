@using PresalesApp.Shared

<RadzenDropDown Name="PositionDropDown" TValue="Position" Value="@Position"
                ValueChanged="@(position => _OnPositionChanged(position))" Data="@_Positions">
    <Template>
        @(Localization[$"{nameof(PresalesApp.Shared.Position)}{(Position)context}Text"])
    </Template>
    <ValueTemplate>
        @(Localization[$"{nameof(PresalesApp.Shared.Position)}{(Position)context}Text"])
    </ValueTemplate>
</RadzenDropDown>