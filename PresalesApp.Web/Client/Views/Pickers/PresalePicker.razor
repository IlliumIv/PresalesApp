<RadzenDropDown Name="PresaleDropDown" TValue="string" Value="@SelectedPresale"
                ValueChanged="@(args => _OnPresaleChanged(args))" Data="@_Names">
    <Template>
        @(Helpers.GetFirstAndLastName(context))
    </Template>
    <ValueTemplate>
        @(Helpers.GetFirstAndLastName(context))
    </ValueTemplate>
</RadzenDropDown>