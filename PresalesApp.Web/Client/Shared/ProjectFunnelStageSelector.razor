@using PresalesApp.Web.Shared

<RadzenDropDown Disabled=@_disabled Data=@_stages @bind-Value=@_selectedStage Change="@DropDown0Change" Style="width: 120px" ClearSearchAfterSelection="true">
    <Template>
        <img src="icons/icons8-waiting.gif" width="20px" style="@_imgDisplay">
        @(((FunnelStage)context).GetLocalizedName(Localization))
    </Template>
</RadzenDropDown>